using System.Collections;
using UnityEngine;

/*
public class HealthExtension : MonoBehaviour
{
    public Health main;
    public string identifier;

    public new Interaction Damage(int amount, DamageType type, MonoBehaviour source, string customIdentifier = null)
    => main.ChangeHealth(-amount, type, source, this, customIdentifier);

}


 */